{
  "buildCommand": "pnpm install && pnpm --filter @workspace/portfolio-site run build",
  "outputDirectory": "vercel-output/dist",
  "framework": "vite",
  "installCommand": "echo 'Dependencies installed by buildCommand'",
  "regions": ["iad1"]
}
